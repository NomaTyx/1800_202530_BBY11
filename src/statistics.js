import { db } from "./firebaseConfig.js";
import { doc, collection, getDocs, addDoc, serverTimestamp } from "firebase/firestore";

// I have yet to code anything, because I'm trying to figure out js.

